# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.3.0

# 数据库
sqlalchemy==2.0.30
psycopg2-binary==2.9.9   # 生产 PostgreSQL；本地 SQLite 无需

# 文档解析
python-docx==1.1.2
openpyxl==3.1.2
PyMuPDF==1.24.5          # PDF 解析

# RAG / 向量库
qdrant-client==1.9.1
# bge embedding 体积大（依赖 torch），按需安装后将 EMBEDDER=bge：
# sentence-transformers==3.0.1

# LLM（境外）。设置 ANTHROPIC_API_KEY 且 LLM_PROVIDER=claude 时启用
anthropic==0.28.0

# 报告导出
# python-docx 复用上面

# 异步任务（生产；离线开发可不安装，配置项会自动走 eager 模式）
celery==5.4.0
redis==5.0.4

# 测试
pytest==8.2.0
